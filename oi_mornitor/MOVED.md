# 已迁移

本目录已搬到：

**`/Users/maotouying/frontend/code/1.operations/auto-script/discord-collector/oi_mornitor`**

请在新位置开发与启动（`pnpm run oi:dev` 或 `python run.py --dev`）。  
卡片外送、币种计价、收益率结算与 `discord-collector` 同仓联调。

旧目录可保留作对照；确认无误后可自行删除本目录（含本地 `venv` / `data/*.db`）。
